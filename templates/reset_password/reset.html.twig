{% extends 'base.html.twig' %}

{% block title %}Réinitailiser le mot de passe
{% endblock %}

{% block body %}
	<h1>Réinitailiser le mot de passe</h1>

	{{ form_start(resetForm) }}
	{{ form_row(resetForm.plainPassword) }}
	<div class="d-grid gap-2">
		<button class="btn btn-primary btn-lg mt-5">Réinitailiser le mot de passe</button>
	</div>
	{{ form_end(resetForm) }}
{% endblock %}
